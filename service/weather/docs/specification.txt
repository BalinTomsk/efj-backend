weather-station-pusher — Full Specification
============================================
Version: 1.0.0
Last updated: 2026-06-22

This document is the single source of truth for recreating the service from scratch.
It must be kept in sync with every source file change.


PROJECT IDENTITY
----------------
Service name : debian-weather
Language     : Java 21
Build        : Maven
Framework    : Spring Boot 3.5.16
Main class   : com.fishfind.weather.WeatherStationPusherApplication
Artifact     : com.fishfind:weather-station-pusher:1.0.0


GOAL
----
- Poll up to 1400 US weather stations from MSSQL (dbo.vwWeatherForecastToDay).
- Fetch raw JSON forecast data from Open-Meteo for each station.
- Store the raw JSON as-is into dbo.ows_meteo with source type 2. Do NOT parse JSON in Java.
- After each full pass, run post-processing stored procedures in order:
    1. dbo.spPushSpeciesFromLakeToStation
    2. dbo.spTotalUpdateProbability
- Log failures and skipped unpublished-source events; do not disable stations automatically.
- Sleep until next midnight between cycles.
- Expose a health endpoint on port 8081 for Docker/Kubernetes health checks.


DEPENDENCIES (pom.xml)
-----------------------
Parent    : spring-boot-starter-parent 3.5.16
Java      : 21

Runtime:
  spring-boot-starter
  spring-boot-starter-web
  spring-boot-starter-actuator
  spring-boot-starter-jdbc
  spring-boot-starter-aop
  com.microsoft.sqlserver:mssql-jdbc  (managed by Spring Boot BOM)
  io.github.resilience4j:resilience4j-spring-boot3:2.4.0
  io.github.cdimascio:dotenv-java:3.2.0
  slf4j-api                           (via Spring Boot)
  logback-classic                     (via Spring Boot)
  net.logstash.logback:logstash-logback-encoder:7.4

Test:
  spring-boot-starter-test

Build plugins:
  org.cyclonedx:cyclonedx-maven-plugin:2.9.2   (SBOM at target/bom.json, bound to package phase)
  org.owasp:dependency-check-maven:12.2.2      (CVE scan; run: mvn dependency-check:check)
    failBuildOnCVSS: 7
    suppressions:   .owasp-suppressions.xml
    NVD API key:    NVD_API_KEY env var


PACKAGE LAYOUT
--------------
com.fishfind.weather
├── WeatherStationPusherApplication.java   main class + dotenv bootstrap
├── domain
│   └── StationRef.java                   record: mli, latitude, longitude, state
├── repo
│   ├── WeatherDataRepository.java
│   └── WeatherStationRepository.java
└── service
    ├── ConsoleDebugRunner.java            --console one-shot mode
    ├── OpenMeteoFetcher.java              HTTP client (resilience4j + 429 Retry-After)
    ├── ProcessingOutcome.java             enum PROCESSED / SKIPPED / FAILED
    ├── RateLimitedException.java          IOException raised on exhausted 429 retries
    ├── StationPostProcessingService.java  stored procedure runner
    ├── StationProcessorBase.java          template method; returns ProcessingOutcome
    ├── StationProcessorOpen.java          fetch + persist flow
    └── StationWorker.java                 background worker + graceful shutdown + post-proc gate


REQUIRED FILES
--------------
pom.xml
src/main/java/com/fishfind/weather/WeatherStationPusherApplication.java
src/main/java/com/fishfind/weather/domain/StationRef.java
src/main/java/com/fishfind/weather/repo/WeatherDataRepository.java
src/main/java/com/fishfind/weather/repo/WeatherStationRepository.java
src/main/java/com/fishfind/weather/service/ConsoleDebugRunner.java
src/main/java/com/fishfind/weather/service/OpenMeteoFetcher.java
src/main/java/com/fishfind/weather/service/ProcessingOutcome.java
src/main/java/com/fishfind/weather/service/RateLimitedException.java
src/main/java/com/fishfind/weather/service/StationPostProcessingService.java
src/main/java/com/fishfind/weather/service/StationProcessorBase.java
src/main/java/com/fishfind/weather/service/StationProcessorOpen.java
src/main/java/com/fishfind/weather/service/StationWorker.java
src/main/resources/application.yml
src/main/resources/logback-spring.xml
.env.example
.owasp-suppressions.xml
Dockerfile
.dockerignore
docs/specification.txt


STARTUP / CREDENTIAL LOADING
-----------------------------
Before Spring initialises:
1. Read DB_URL, DB_USERNAME, DB_PASSWORD from process environment or JVM system properties.
2. If any are missing, attempt to load from dotenv:
   - if DOTENV_PATH is set and non-blank → load that path
   - otherwise, if .env exists in the working directory → load it
   - ignore missing or malformed dotenv files
3. Collect dotenv values as a Map and register them via SpringApplication.setDefaultProperties()
   so secrets (especially DB_PASSWORD) are never written to the JVM-global System.setProperty()
   table where they would be visible to heap dumps and diagnostic tools.
   Real process env / JVM system properties always win (they take precedence over default properties).


CONFIGURATION (application.yml)
--------------------------------
spring:
  datasource:
    url: ${DB_URL}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
    driver-class-name: com.microsoft.sqlserver.jdbc.SQLServerDriver
  lifecycle:
    timeout-per-shutdown-phase: 30s     # bound graceful-shutdown wait

server:
  port: 8081                          # single listener: only actuator endpoints (no controllers)
  shutdown: graceful

resilience4j:
  retry:
    instances:
      sqlRetry:
        max-attempts: 3
        wait-duration: 2s
        retry-exceptions: [DataAccessException, SQLException]
      openMeteo:                         # HTTP fetch retry
        max-attempts: 4
        wait-duration: 1s
        enable-exponential-backoff: true
        exponential-backoff-multiplier: 2
        enable-randomized-wait: true
        randomized-wait-factor: 0.5
        retry-exceptions: [java.io.IOException]
        ignore-exceptions:               # 404 / already-honoured 429 / open breaker
          - java.io.FileNotFoundException
          - com.fishfind.weather.service.RateLimitedException
          - io.github.resilience4j.circuitbreaker.CallNotPermittedException
  circuitbreaker:
    instances:
      sqlBreaker:
        sliding-window-size: 10
        minimum-number-of-calls: 5
        failure-rate-threshold: 50
        wait-duration-in-open-state: 30s
      openMeteo:
        sliding-window-size: 20
        minimum-number-of-calls: 10
        failure-rate-threshold: 50
        wait-duration-in-open-state: 60s
        permitted-number-of-calls-in-half-open-state: 3
        record-exceptions: [java.io.IOException]
        ignore-exceptions: [java.io.FileNotFoundException]
  ratelimiter:
    instances:
      openMeteo:
        limit-for-period: 5
        limit-refresh-period: 1s
        timeout-duration: 10s

weather:
  worker:
    connect-timeout-ms: 15000
    read-timeout-ms: 30000
    open-meteo-base-url: https://api.open-meteo.com/v1/forecast
    rate-limit:                          # inline 429 Retry-After handling
      max-retries: 2
      default-wait-ms: 5000
      max-wait-ms: 60000
    max-response-bytes: 5242880          # cap on fetched payload size (5 MB)
    post-processing:
      max-failure-rate: 0.5              # skip post-processing above this failure rate

management:
  endpoints:
    web:
      exposure:
        include: health
  endpoint:
    health:
      probes:
        enabled: true
      show-details: never

logging:
  level:
    root: INFO
    com.fishfind.weather: INFO


HEALTH ENDPOINT
---------------
Spring Boot Actuator serves on port 8081 — the application's ONLY HTTP listener
(server.port: 8081, no separate management port, nothing listens on 8080).
The app has no controllers, so this listener serves only the actuator endpoints;
every other path returns 404.

  GET /actuator/health           → overall status incl. DB indicator ({"status":"UP"})
  GET /actuator/health/liveness  → liveness probe (Docker HEALTHCHECK + k8s livenessProbe)
  GET /actuator/health/readiness → Kubernetes readiness probe

Kubernetes probe example:
  livenessProbe:
    httpGet:
      path: /actuator/health/liveness
      port: 8081
    initialDelaySeconds: 60
    periodSeconds: 30
  readinessProbe:
    httpGet:
      path: /actuator/health/readiness
      port: 8081
    initialDelaySeconds: 60
    periodSeconds: 10


WORKER BEHAVIOUR
----------------
Thread
  Launched by StationWorker (ApplicationRunner).
  Thread name: weather-data-worker-open
  Non-daemon thread.
  If app starts with --console: do not launch the background thread.
  Pauses (per-station pacing and between-cycle waits) go through a protected sleep(long ms)
  method so tests can subclass and run without real waits.

Graceful shutdown
  - A volatile `running` flag and the loop's interrupt check let the worker stop cleanly.
  - @PreDestroy shutdown(): set running=false, interrupt the worker thread, join up to 25s.
  - If a stop is requested mid-cycle the loop breaks and post-processing is SKIPPED.

Per-cycle loop
  1. Load US stations from dbo.vwWeatherForecastToDay (max 1400).
  2. Compute target delay: max(8_hours_ms / stationCount, 2000). If count <= 1, use 2000 ms.
  3. For each station (unless stopping): process() → tally PROCESSED/SKIPPED/FAILED →
     sleep (targetDelay - actualProcessingTime). Skip sleep if negative.
  4. Post-processing gate (maybeRunPostProcessing):
       attempted = processed + failed   (skipped stations excluded)
       if attempted > 0 and failed/attempted > weather.worker.post-processing.max-failure-rate
         → log ERROR (cycle degraded) and SKIP post-processing
       else → run post-processing procedures
     Rationale: a high failure rate (e.g. open circuit / mass SQL failure) must not recompute
     probabilities from partial data; legitimate no-feed skips never block post-processing.
  5. Sleep until next local midnight (skip if already past boundary).


CONSOLE / DEBUG MODE (--console)
---------------------------------
Activated by --console command-line arg (@Order(0); exits process when done).
Optional: --station=<MLI> to filter to one station.
Runs exactly one processing pass; no background thread started.
Process termination goes through a protected exit(int) method (default System.exit) so tests
can exercise the console path without terminating the JVM.


STATION QUERY (WeatherStationRepository)
-----------------------------------------
  SELECT TOP 1400 mli, lat, lon, state
  FROM dbo.vwWeatherForecastToDay
  WHERE country = 'US'

Model: StationRef(String mli, double latitude, double longitude, String state)


HTTP FETCH (OpenMeteoFetcher)
------------------------------
Transport: HttpURLConnection, method GET.
Base URL: weather.worker.open-meteo-base-url (default https://api.open-meteo.com/v1/forecast).
  Overridable so tests can target a local server; production default is unchanged.
Connect timeout: weather.worker.connect-timeout-ms (default 15 000 ms).
Read timeout: weather.worker.read-timeout-ms (default 30 000 ms).
User-Agent: Chrome-like, e.g.
  "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.NN (KHTML, like Gecko) Chrome/124.0 Safari/537.NN"
  The two 537.NN WebKit/Safari build numbers are randomised in [11, 97]; seeded by the calendar
  day so they stay stable for a whole day and change from one day to the next
  (OpenMeteoFetcher.currentUserAgent).

Resilience (fetch is annotated @Retry + @CircuitBreaker + @RateLimiter, instance "openMeteo"):
  HTTP 200 → read response body as UTF-8 verbatim (no post-processing of the body), BUT:
             - body larger than weather.worker.max-response-bytes → IOException (retried)
             - body whose first non-whitespace char is not '{' → IOException (retried).
               This is a cheap shape guard, NOT JSON parsing: it keeps HTML error pages /
               captive portals out of ows_meteo; the accepted payload is still stored raw.
  HTTP 404 → throw FileNotFoundException (not retried; maps to SKIPPED upstream)
  HTTP 429 → honour Retry-After inline (delta-seconds or HTTP-date), clamped to
             [0, weather.worker.rate-limit.max-wait-ms]; retry up to rate-limit.max-retries.
             If still 429 → throw RateLimitedException (recorded by the breaker).
             Interrupt during the Retry-After wait → re-set interrupt flag and throw
             RateLimitedException (excluded from Resilience4j retry, so shutdown is not
             delayed by further backoff attempts).
  Other non-200 / timeout → throw IOException (retried with exponential backoff)
  Resilience4j AOP is active only on the Spring-managed bean at runtime (not in direct unit tests).

URL shape:
  https://api.open-meteo.com/v1/forecast
    ?latitude={lat}&longitude={lon}
    &hourly=temperature_2m,relative_humidity_2m,precipitation_probability,
            pressure_msl,wind_speed_10m,wind_direction_10m,weather_code,rain
    &daily=temperature_2m_max,temperature_2m_min
    &timezone=auto


PERSISTENCE (WeatherDataRepository)
-------------------------------------
Save raw JSON:
  void saveStationData(String mli, String jsonData)
  - Reject blank mli → throw IllegalArgumentException
  - Blank/null jsonData → no-op
  - Runs in a transaction; @Retry(sqlRetry) + @CircuitBreaker(sqlBreaker)
  - If the UPDATE matches 0 rows (no ows_meteo row for that mli), log a WARNING
    with mli and payload size — the payload is dropped and must not vanish silently.
  SQL:
    UPDATE dbo.ows_meteo
    SET type = 2, ows = ?, stamp = GETDATE()
    WHERE mli = ?

Post-processing procedures:
  pushSpeciesFromLakeToStation()  → EXEC dbo.spPushSpeciesFromLakeToStation
  totalUpdateProbability()        → EXEC dbo.spTotalUpdateProbability
  Each runs in a transaction; each protected by Resilience4j retry + circuit breaker.
  Procedures may emit incidental result sets — drain with PreparedStatementCallback + getMoreResults.


PROCESSING PIPELINE
-------------------
StationProcessorBase (template method):
  process(StationRef) is final; calls processStation() inside try/catch and returns a
  ProcessingOutcome (PROCESSED / SKIPPED / FAILED) the worker uses for the post-processing gate:
    success              → PROCESSED
    FileNotFoundException → SKIPPED; log info "Skipping US station with no published Open-Meteo source. ..."
    any other exception   → FAILED;  log warning "US station processing failed. station={mli} state={state}"

StationProcessorOpen:
  1. Fetch JSON from Open-Meteo using station lat/lon.
  2. Log save start with payload byte count.
  3. Update dbo.ows_meteo via WeatherDataRepository.
  4. Log station processed.

StationPostProcessingService calls procedures in this exact order:
  1. dbo.spPushSpeciesFromLakeToStation
  2. dbo.spTotalUpdateProbability


TIMING MODEL
------------
  1400 stations, 8h budget → delay ≈ 28_800_000 / 1400 = 20_571 ms per station
  Actual delay             → targetDelay − actualProcessingTime; never negative
  Between cycles           → sleep until next local midnight; skip if already past boundary


ERROR HANDLING
--------------
  Missing DB_URL / DB_USERNAME / DB_PASSWORD → Spring datasource fails at startup
  Blank mli during save                      → throw IllegalArgumentException
  Blank / null JSON payload                  → skip save
  HTTP 404 from Open-Meteo                   → FileNotFoundException; log skip; outcome SKIPPED
  HTTP 429 from Open-Meteo                   → honour Retry-After inline; if exhausted RateLimitedException → FAILED
  Interrupted during Retry-After wait        → RateLimitedException (no further retry); interrupt flag re-set
  200 body oversized or not a JSON object    → IOException; retried (backoff); if exhausted → FAILED
  UPDATE matches 0 ows_meteo rows            → log WARNING (payload dropped); still counts as PROCESSED
  HTTP non-200 other than 404 / timeout      → IOException; retried (backoff); if exhausted → FAILED
  Per-station processing exception           → log warning; outcome FAILED; continue
  SQL write failure                          → Resilience4j retry/circuit breaker; then propagates → FAILED
  Open Open-Meteo circuit                    → CallNotPermittedException fast-fail → FAILED
  High cycle failure rate (> max-failure-rate) → log ERROR; SKIP post-processing for the cycle
  Post-processing emits result sets          → drain with getMoreResults; continue normally
  Stop requested mid-cycle (@PreDestroy)     → break loop; skip post-processing; thread joins (<=25s)
  Worker thread interrupted                  → re-set interrupt flag; stop worker thread
  Unexpected worker loop exception           → log error; continue loop

Never: disable stations automatically; stop the worker for a single station failure; parse JSON.


LOGGING
-------
Root level: INFO / com.fishfind.weather: INFO
Format: structured JSON via logstash-logback-encoder.
Every log line: timestamp, level, service (debian-weather), logger_name, thread_name, message.
MDC field correlationId appears automatically when set.

Appenders (logback-spring.xml):
  CONSOLE — LogstashEncoder, stdout.
  FILE    — LogstashEncoder, active: logs/weather.log,
            rolls daily to logs/weather-%d{yyyy-MM-dd}.log.gz,
            maxHistory=7, totalSizeCap=500MB.

Required log events:
  - Background worker thread start
  - Supported station count
  - Calculated time-budget delay
  - Fetch success per station
  - Save start (with payload byte count)
  - Station processed
  - Post-processing procedure execution
  - Skip due to unpublished source (info)
  - Station processing failure (warning)
  - Cycle completion with next run time


DOCKER
------
Build stage  : maven:3.9.9-eclipse-temurin-21, runs mvn -B -DskipTests package
Runtime stage: eclipse-temurin:21-jre, copies jar to /app/weather-station-pusher.jar

Security:
  - Non-root user: groupadd appgroup (gid 1001) + useradd appuser (uid 1001, no home)
  - /app/logs created (mkdir -p) and chowned to appuser:appgroup so Logback file appender can write
  - jar is chowned to appuser:appgroup; USER appuser set before EXPOSE
  - ENTRYPOINT uses "exec java ..." so Java replaces the shell and receives SIGTERM as PID 1
  - curl installed in the runtime stage for HEALTHCHECK (eclipse-temurin:21-jre ships
    neither curl nor wget; a wget-based HEALTHCHECK would always report unhealthy)

  RUN apt-get update && \
      apt-get install -y --no-install-recommends curl && \
      rm -rf /var/lib/apt/lists/*
  EXPOSE 8081
  HEALTHCHECK --interval=30s --timeout=5s --start-period=60s --retries=3 \
    CMD curl -sf http://localhost:8081/actuator/health/liveness || exit 1
  ENTRYPOINT ["sh", "-c", "exec java $JAVA_OPTS -jar /app/weather-station-pusher.jar"]

The HEALTHCHECK targets /actuator/health/liveness, NOT the overall /actuator/health:
the overall endpoint includes the DataSource indicator, so a transient DB outage would
mark the container unhealthy even though the worker sleeps and recovers on its own.

.dockerignore must exclude: .git, .idea, target, docs, .env
Do NOT bake a real .env into the image.


RUN MODES
---------
  Normal daemon (loops indefinitely):
    java -jar target/weather-station-pusher-1.0.0.jar

  One-shot pass through all stations, then exit:
    java -jar target/weather-station-pusher-1.0.0.jar --console

  One-shot for a single station (by MLI identifier):
    java -jar target/weather-station-pusher-1.0.0.jar --console --station=<MLI>


TESTING
-------
Unit tests (JUnit 5 + Mockito + AssertJ, all bundled by spring-boot-starter-test) cover all
functional classes. No Spring context is started; collaborators are mocked.

  WeatherStationPusherApplicationTest  addIfMissing fallback logic (env/sysprop/dotenv branches)
  StationRefTest                       record accessors + value equality
  OpenMeteoFetcherTest                 real loopback HttpServer: verbatim 200 body, daily UA +
                                       its [11,97] build numbers, coordinate/query params,
                                       404→FileNotFoundException, other non-200→IOException,
                                       429 Retry-After then success, exhausted 429→RateLimitedException
  WeatherStationRepositoryTest         query SQL + RowMapper column mapping
  WeatherDataRepositoryTest            save SQL/args, blank-mli reject, blank-payload skip,
                                       procedure execution + result-set drain, fallback wrappers
  StationProcessorBaseTest             template-method outcome (PROCESSED/SKIPPED/FAILED) via
                                       captured log events
  StationProcessorOpenTest             fetch+persist→PROCESSED, FileNotFound→SKIPPED, IOException→FAILED
  StationPostProcessingServiceTest     stored-procedure invocation order
  StationWorkerTest                    delay budget math, millisUntilNextMidnight bounds, runOnce
                                       all/filtered, skipped-don't-block + degraded-cycle post-proc
                                       gate, stop-requested short-circuit, shutdown flag, console
  ConsoleDebugRunnerTest               no-op without --console; runs + exits with/without --station

Testability seams used by the tests (production behaviour unchanged):
  - OpenMeteoFetcher: weather.worker.open-meteo-base-url
  - StationWorker:    protected sleep(long)
  - ConsoleDebugRunner: protected exit(int)


BUILD COMMANDS
--------------
  mvn -DskipTests package       # build jar
  mvn test                      # run all tests
  mvn test -Dtest=MyTestClass   # run one test class
  docker build -t weather-station-pusher:1.0.0 .


EXPLICITLY NOT IMPLEMENTED
--------------------------
Do not add these unless explicitly requested:
  - Weather Underground worker
  - Canadian station processing
  - Windows Service hosting
  - Java-side parsing of weather JSON into hourly/daily rows
  - Automatic station disabling
  - API key handling for Open-Meteo


RECREATION CHECKLIST
---------------------
 1. Maven Java 21 Spring Boot project; coordinates com.fishfind:weather-station-pusher:1.0.0
 2. Add dependencies: web, actuator, JDBC, AOP, MSSQL, Resilience4j, dotenv, logback
 3. Add build plugins: cyclonedx-maven-plugin:2.9.2 (bound to package), dependency-check-maven:12.2.2
 4. Implement dotenv bootstrap in WeatherStationPusherApplication using setDefaultProperties()
    (secrets must NOT be written to System.setProperty — use Spring default properties map)
 5. StationRef record: mli, latitude, longitude, state
 6. WeatherStationRepository — vwWeatherForecastToDay US query
 7. OpenMeteoFetcher — HttpURLConnection; honest User-Agent; verbatim UTF-8 body (no replace)
 8. WeatherDataRepository — UPDATE dbo.ows_meteo, two stored procedure methods, Resilience4j
 9. StationProcessorBase — template method exception handling
10. StationProcessorOpen — fetch + save flow
11. StationPostProcessingService — exact procedure order
12. StationWorker — background thread, 8-hour dynamic delay, midnight sleep
13. ConsoleDebugRunner — --console + --station one-shot mode
14. application.yml (datasource, resilience4j, weather.worker, management, logging)
15. logback-spring.xml (CONSOLE + FILE appenders, LogstashEncoder, service=debian-weather)
16. .env.example with trustServerCertificate=false
17. .owasp-suppressions.xml stub
18. .dockerignore
19. Dockerfile: non-root appuser (uid 1001), curl installed for HEALTHCHECK,
    exec java entrypoint, EXPOSE 8081 + HEALTHCHECK (curl -sf)
20. Unit tests for all functional classes (see TESTING) using the sleep/exit/base-url seams
21. Verify: mvn test
