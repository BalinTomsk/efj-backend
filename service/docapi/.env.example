DB_URL=jdbc:sqlserver://host.docker.internal:1433;databaseName=your_database;encrypt=true;trustServerCertificate=true
DB_USERNAME=your_username
DB_PASSWORD=your_password

# Dedicated MySQL datasource for the news read endpoints only (GET /news/{id}, /news/list,
# /news/default) -- see JdbcStoreConfig.mysqlNewsJdbcTemplate and envfish-db/mysql/.
MYSQL_NEWS_URL=jdbc:mysql://your-mysql-host:3306/your_database?useSSL=true&serverTimezone=UTC
MYSQL_NEWS_USERNAME=your_username
MYSQL_NEWS_PASSWORD=your_password

# Optional: if you want dotenv-java to read a file inside the container instead of standard env vars.
# DOTENV_PATH=/app/.env

# Optional JVM flags
# JAVA_OPTS=-Xms256m -Xmx512m
